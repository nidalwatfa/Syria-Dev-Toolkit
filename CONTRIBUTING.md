# 🤝 إرشادات المساهمة في Syria Dev Toolkit

نشكرك على اهتمامك بالمساهمة في هذا المستودع! مساهمتك لا تقدر بثمن في إثراء المحتوى لخدمة المجتمع التقني السوري.

## 📝 كيف يمكنني المساهمة؟

يمكنك المساهمة في هذا المشروع بعدة طرق:

1.  **إضافة مصادر جديدة:** (روابط لأدوات، دورات، مجتمعات).
2.  **تحسين الوثائق:** تصحيح الأخطاء الإملائية، أو تحسين التنسيق، أو توضيح النصوص.
3.  **الإبلاغ عن أخطاء أو روابط مكسورة:** عبر فتح مشكلة (Issue).

## 💡 خطوات تقديم مساهمة (Pull Request)

لضمان سهولة مراجعة مساهمتك، اتبع الخطوات التالية:

### 1. الإبلاغ عن مشكلة (Issues)

* إذا كنت تجد رابطًا مكسورًا أو فكرة تطوير عامة، يرجى **فتح مشكلة جديدة (Issue)** أولاً.
* استخدم عناوين واضحة لوصف المشكلة أو الاقتراح.

### 2. عملية طلب السحب (Pull Request - PR)

1.  **تفرع المستودع (Fork):** قم بعمل Fork للمستودع `Syria-Dev-Toolkit`.
2.  **إنشاء فرع جديد (Branch):** أنشئ فرعًا جديدًا لمساهمتك. يجب أن يكون اسم الفرع وصفيًا (مثلاً: `feature/add-python-links` أو `fix/broken-link-docs`).
    ```bash
    git checkout -b feature/your-contribution-name
    ```
3.  **إجراء التغييرات:** قم بإضافة أو تعديل الروابط والمحتوى في ملف `README.md` أو غيره من الملفات ذات الصلة.
4.  **الالتزام (Commit):** قم بالالتزام بالتغييرات برسالة واضحة تلخص ما فعلته.
    ```bash
    git commit -m "feat: Added new set of resources for web development"
    ```
5.  **الرفع (Push):** ارفع التغييرات إلى المستودع الخاص بك.
    ```bash
    git push origin feature/your-contribution-name
    ```
6.  **فتح طلب سحب (Open PR):** افتح طلب سحب (Pull Request) من الفرع الخاص بك إلى الفرع `main` في المستودع الأصلي.

### 📜 إرشادات التنسيق

**لإضافة المصادر في ملف `README.md`، يرجى الالتزام بالصيغة التالية:**

* استخدم تنسيق قائمة Markdown (علامة النجمة `*`).
* تأكد من وجود رابط فعال.
* **الصيغة المطلوبة:**
    ```markdown
    * [عنوان المصدر] - وصف موجز وواضح للمصدر (مثال: دورة فيديو، أداة مجانية، مجتمع).
    ```
* لا تستخدم الروابط المختصرة (Shortened URLs).

### 👮 مدونة قواعد السلوك

لضمان بيئة عمل إيجابية، نتوقع من جميع المساهمين الالتزام بـ [CODE_OF_CONDUCT.md](CODE_OF_CONDUCT.md). جميع التفاعلات يجب أن تكون مهنية ومحترمة.

